spring:
  application:
    name: ebenezer-service
  cloud:
    config:
      enabled: false

server:
  port: 8085

openWeather:
  baseUrl: https://api.openweathermap.org/data/2.5/weather

app:
  id: ${appid}

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/